import 'package:flutter/material.dart';

class PortFolioTextButton extends StatelessWidget {
  const PortFolioTextButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('data');
  }
}
